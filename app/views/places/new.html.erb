<%if @current_user%>

<h1>New Place</h1>

<%= form_with model: @place do |form| %>
  <p>
    <%= form.label "name" %>
    <%= form.text_field "name" %>
  </p>
  
  <p>
    <button class="btn btn-success py-2">Create New Place</button>
  </p>
<% end %>

<%else%>

Login to create a new memories!

<%end%>